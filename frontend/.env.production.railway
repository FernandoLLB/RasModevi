# Variables para el build de Railway (store + frontend en la nube)
# VITE_STORE_API_URL queda vacío — el frontend se sirve desde el mismo origen (Railway)
VITE_STORE_API_URL=
# VITE_DEVICE_API_URL apunta a la Pi via Cloudflare Tunnel
VITE_DEVICE_API_URL=https://pi.modevi.es

{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && uvicorn main_store:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/store/categories",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
